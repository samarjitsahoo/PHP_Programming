<?php 

// $a = readfile("myfile.txt"); //Reads and gives the number of characters read
// echo $a;

// readfile("myfile.txt");  //Automatically Reads and displays the content in the Output buffer
readfile("sample.html");  //Reads the the HTML file and automatically throws the content to the buffer to display

?>